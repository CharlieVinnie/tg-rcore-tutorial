## 0.1.0-preview.2

Modified to NOT assert `MAP_ANON` in `mmap`

Modified to expect `path` and `count` instead of \0 ended strings in `open()`.

## 0.1.0-preview.3

Fix \0 bug in user keyshow.rs and font.rs; pull new user crate

## 0.1.1-preview.1

Fix versions; use newest syscall crate